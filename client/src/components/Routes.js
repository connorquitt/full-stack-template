import React from 'react';
import { Switch, Route, Redirect } from 'react-router-dom';

/*
const Routes = () => {

  return (
    <Switch>

      <Route path='/example/:id'>
        {isLoggedIn ? <ExampleId /> : <Redirect to="/login" />}
      </Route>

      <Route path="/example">
        {isLoggedIn ? <Example /> : <Redirect to="/login" />}
      </Route>

    </Switch>
  );
};

export default Routes;
*/